{{!< default}}

<div class="content-area">
    <main class="site-main">
        {{#author}}
            {{> author-details}}
        {{/author}}
        <div class="post-feed">
            {{#foreach posts}}
                {{> "loop"}}
            {{/foreach}}
        </div>
        {{pagination}}
    </main>
</div>